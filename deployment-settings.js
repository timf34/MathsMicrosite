// Change true to false and commit in GitHub to open the site to everyone.
// Vercel applies the change on its next automatic deployment.
export const PASSWORD_PROTECTION_ENABLED = true;
export const SITE_PASSWORD = 'HAMILTON';
